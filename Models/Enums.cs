namespace EmployeePortal.Models
{
    public enum RoleType { Employee = 0, Admin = 1 }
    public enum LeaveType { Casual = 0, Sick = 1, Other = 2 }
    public enum RequestStatus { Pending = 0, Approved = 1, Rejected = 2 }

}
